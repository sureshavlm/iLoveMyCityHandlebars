<div class="container">
	<div class="row">
		{{#each imageCount }}
			<div class="col-md-6">
				<div class="thumbnail">
					<img class="img-responsive" 
					src="/images/{{../city}}/{{../city}}-{{this}}.jpg">
				</div>
			</div>
		{{/each}}
	</div>
</div>